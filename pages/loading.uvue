<template>
	<!-- #ifdef APP -->
	<scroll-view style="flex:1">
	<!-- #endif -->
		<view class="loading">
			<image src="/static/loading_bg.png" style="width:100%;height:100%;"></image>
		</view>
	<!-- #ifdef APP -->
	</scroll-view>
	<!-- #endif -->
</template>

<script setup>
	onMounted(()=>{
		setTimeout(()=>{
			uni.switchTab({
				url:"/pages/index/index"
			})
		},2000)
	})
</script>

<style>
	.loading{
		width:100%;
		height:100%;
		/* background-color: antiquewhite; */
		/* background-image: url(/static/loading_bg.png);
		background-size: 100% 100%; */
		/* background:#ffffff url('/static/loading_bg.png') no-repeat left top; */
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		font-size: 60rpx;
		color: red;
		font-weight: bold;
	}
</style>
