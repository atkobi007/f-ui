<!-- 高图片[热度+集数] / 标题 / 类型-->
<!-- 
  -----------
 |          |
 |          |
 |          |
 | 热     集|
 -----------
 标题标题标题
 短剧
 -->
<template>
	<view class="item_container">
		<view class="item_content" v-if="!data.isEmpty">
			<image :src="data.url" style="width:100%;height:100%;"></image>
			<view class="item_info">
				<view class="item_info_hot">
					<image src="/static/logo.png" style="width:30rpx;height:30rpx;"></image>
					<text class="item_info_hot_text">1024</text>
				</view>
				<text class="item_info_hot_text">8集全</text>
			</view>
		</view>
		<text class="item_title" v-if="!data.isEmpty">{{data.title}}</text>
		<text class="item_title_sub" v-if="!data.isEmpty">剧情</text>
	</view>
</template>

<script setup>
	import { ListSubItem } from '../../http/request';
	interface Props {
	  data:{
		  title: string
		  url: string
		  //点位,如果数组长度是单个的就点位一个空的位置(不显示出来)
		  isEmpty:boolean
	  }
	}
	const props = withDefaults(defineProps<Props>(), {
	  data:{
		  title:"",
		  url:"",
		  isEmpty:true,
	  }
	})
	
</script>

<style scoped>
 .item_container{
	width: 46%;
	height: 560rpx;
	position: relative;
	display: flex;
	flex-direction: column;
	margin-bottom: 40rpx;
 }
 
 .item_content{
	 position: relative;
	 flex:1;
 }
 
 .item_info{
	 padding: 6rpx 10rpx;
	 position: absolute;
	 bottom:0px;
	 height:50rpx;
	 width:100%;
	 display: flex;
	 flex-direction: row;
	 justify-content: space-between;
	 align-items: center;
	 background-image: linear-gradient(to top,rgba(0,0,0,0.6),rgba(0,0,0,0.01))
 }
 .item_info_hot{
	 display: flex;
	 flex-direction: row;
	 align-items: center;
 }
 .item_info_hot_text{
	 font-size: 20rpx;
 }
 
 .item_title{
	 overflow: hidden;
	 text-overflow: ellipsis;
	 white-space: nowrap;
	 font-size: 30rpx;
	 height:50rpx;
	 padding-top:10rpx;
 }
 .item_title_sub{
 	 overflow: hidden;
 	 text-overflow: ellipsis;
 	 white-space: nowrap;
 	 font-size: 26rpx;
 	 height:30rpx;
	 color:#ccc;
 }
</style>